# Data (never commit raw archives or the DuckDB database)
data/raw/
data/*.duckdb
data/*.duckdb.wal
# Local scoreboard ledger; deployed instances keep theirs in R2.
data/ledger.json

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Reports: keep the markdown + figures tracked, they're the deliverable.

# OS / editor
.DS_Store
.idea/
.vscode/

# Local secrets for `wrangler dev` and `wrangler types`. The real values
# live in `wrangler secret put`; this file must never be committed.
.dev.vars

# Wrangler / Cloudflare local state
.wrangler/
node_modules/
# Generated by `npx wrangler types` from wrangler.jsonc
worker-configuration.d.ts
